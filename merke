git init
git remote add origin

git status
 zeigt geänderte/hinzugefügte Dateien, müssen zur neuen Revision hinzugefügt werden mit:
git add %Verzeichnis/ Datei%
git commit -m "message mit Änderungen hier hin schreiben"
git push origin %branch (bei uns jetzt master)
